'use strict';

export const add = (a, b) => {
  return a + b;
};

export const sub = (a, b) => {
  return a - b;
};

const division = (a, b) => {
  return a / b;
};

const multi = (a, b) => {
  return a * b;
};

export default {
  division,
  multi,
};
